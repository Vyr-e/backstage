{
  "name": "@backstage/core",
  "version": "0.1.0",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "test": "bun test",
    "run-workers": "bun run src/cli.ts run-workers",
    "run-scheduler": "bun run src/cli.ts run-scheduler"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "winston": "^3.19.0"
  }
}
